import React, { FC } from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({}));

export const ClinicSettings: FC = () => {
    const classes = useStyles();

    return (
        <p>Clinic Settings</p>
    );
};